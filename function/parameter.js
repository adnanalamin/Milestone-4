// Declar function with parameter

// Borgo ar man ber kora

function squer(number) {
  const borgo = number * number;
  console.log(borgo);
}
squer(5);
squer(100);
squer(2);
squer(6);

// Add two number usign function two parameter

function add(num1, num2) {
  const total = num1 + num2;
  console.log(total);
}
add(4, 5);

// Add five number

function addAll(a, b, c, d, e) {
  const total = a + b + c + d + e;
  console.log(total);
}
addAll(2, 2, 2, 2, 2);
